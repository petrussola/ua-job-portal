---
import type { OpeningProps } from "../types/types";
import JobEntry from "./OpeningEntry.astro";

type Props = {
  jobsList: OpeningProps[];
};

const { jobsList } = Astro.props;
---

<ul>
  {
    jobsList.map((job) => {
      return <JobEntry opening={job} />;
    })
  }
</ul>
